# Trace mode extraction (diagnostic)

Prony / log-slope extraction from $\Theta(t) = \mathrm{Tr}(e^{-tH_P^2})$ is **ill-conditioned** for clustered cylinder frequencies.

Marshal `TraceModeDiagnostic.hxx` reports gaps only. **Not a pass gate.**
